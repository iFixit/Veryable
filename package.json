{
  "name": "qa-metrics",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint:types && NODE_ENV=test jest --no-cache --runInBand",
    "start": "npm run build && NODE_ENV=dev node ./out/src/index",
    "debug": "npm run build && NODE_ENV=dev DEBUG='prisma*' node ./out/src/index",
    "build": "tsc -p .",
    "lint:types": "tsc --noEmit -p .",
    "prisma:test_setup": "npx dotenv -c test -- npx prisma db push",
    "prisma:start_setup": "npx dotenv -c dev -- npx prisma db push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/graphql": "^4.6.4",
    "@prisma/client": "^3.1.1",
    "babel": "^6.23.0",
    "babel-core": "^6.26.3",
    "date-and-time": "^1.0.1",
    "dotenv": "^10.0.0",
    "dotenv-expand": "^5.1.0",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.1",
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "prisma": "^3.1.1",
    "rewire": "^5.0.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}